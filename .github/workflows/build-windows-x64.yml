name: Build Windows x64 Binary

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64 upx-ucl

    - name: Install Windows dependencies
      run: |
        # 安装 MinGW-w64 的静态库
        sudo apt-get install -y mingw-w64-tools
        
        # 下载并安装预编译的 Windows 静态库
        mkdir -p /tmp/win-deps
        cd /tmp/win-deps
        
        # 下载 curl 静态库
        wget -q https://curl.se/windows/dl-8.5.0_1/curl-8.5.0_1-win64-mingw.zip
        unzip -q curl-8.5.0_1-win64-mingw.zip
        
        # 下载 OpenSSL 静态库  
        wget -q https://github.com/openssl/openssl/releases/download/openssl-3.2.0/openssl-3.2.0.tar.gz
        tar -xzf openssl-3.2.0.tar.gz
        
        # 下载 libxml2 静态库
        wget -q https://download.gnome.org/sources/libxml2/2.12/libxml2-2.12.3.tar.xz
        tar -xf libxml2-2.12.3.tar.xz

    - name: Build ESurfingClient
      run: |
        echo "🏗️ Starting Windows x64 build process"
        
        echo "📁 Go to esurfing dir"
        cd esurfing
        
        echo "⚙️ CMake configuring with MinGW cross-compilation"
        cmake -G "Unix Makefiles" \
          -DCMAKE_SYSTEM_NAME=Windows \
          -DCMAKE_C_COMPILER=x86_64-w64-mingw32-gcc \
          -DCMAKE_CXX_COMPILER=x86_64-w64-mingw32-g++ \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_EXE_LINKER_FLAGS="-static -static-libgcc -static-libstdc++" \
          -DCMAKE_FIND_LIBRARY_SUFFIXES=".a" \
          -DBUILD_SHARED_LIBS=OFF \
          .
        
        echo "🔨 Building with make"
        make -j$(nproc)
        
        echo "📦 Moving and renaming executable"
        if [ -f "ESurfingClient.exe" ]; then
          mv ESurfingClient.exe ESurfingClient-1.0.0.exe
          echo "✅ Executable renamed to ESurfingClient-1.0.0.exe"
        else
          echo "❌ ESurfingClient.exe not found!"
          exit 1
        fi
        
        echo "🗜️ Compressing with UPX"
        upx --best --lzma ESurfingClient-1.0.0.exe || echo "⚠️ UPX compression failed, continuing..."
        
        echo "📊 File information:"
        ls -la ESurfingClient-1.0.0.exe
        file ESurfingClient-1.0.0.exe

    - name: Verify build output
      run: |
        cd esurfing
        if [ ! -f "ESurfingClient-1.0.0.exe" ]; then
          echo "❌ Build failed: ESurfingClient-1.0.0.exe not found"
          exit 1
        fi
        
        echo "✅ Build successful!"
        echo "📁 Final executable: $(pwd)/ESurfingClient-1.0.0.exe"
        echo "📏 File size: $(stat -c%s ESurfingClient-1.0.0.exe) bytes"

    - name: Create package
      run: |
        echo "📦 Creating Windows x64 package"
        
        # Create package directory
        mkdir -p esurfingclient-windows-x64
        
        # Copy executable
        cp esurfing/ESurfingClient-1.0.0.exe esurfingclient-windows-x64/
        
        # Create README
        cat > esurfingclient-windows-x64/README.txt << 'EOF'
ESurfingClient Windows x64 Binary
================================

This is a statically linked Windows x64 binary of ESurfingClient.

Files:
- ESurfingClient-1.0.0.exe: Main executable (statically linked)

Usage:
  ESurfingClient-1.0.0.exe [options]

Build Information:
- Platform: Windows x64
- Compiler: MinGW-w64 GCC (cross-compiled on Ubuntu)
- Linking: Static (no external DLL dependencies)
- Compression: UPX compressed
- Build Date: $(date)

For more information, visit the project repository.
EOF
        
        echo "📋 Package contents:"
        ls -la esurfingclient-windows-x64/
        
        echo "🗜️ Creating ZIP archive"
        zip -r esurfingclient-windows-x64.zip esurfingclient-windows-x64/

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: esurfingclient-windows-x64
        path: |
          esurfingclient-windows-x64.zip
          esurfing/ESurfingClient-1.0.0.exe
        retention-days: 30

    - name: Generate build summary
      run: |
        echo "## 🏗️ Windows x64 Build Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### ✅ Build Status: SUCCESS" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📦 Artifacts Generated:" >> $GITHUB_STEP_SUMMARY
        echo "- \`esurfingclient-windows-x64.zip\` - Complete package with README" >> $GITHUB_STEP_SUMMARY
        echo "- \`ESurfingClient-1.0.0.exe\` - Standalone executable" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🔧 Build Configuration:" >> $GITHUB_STEP_SUMMARY
        echo "- **Platform**: Windows x64 (cross-compiled on Ubuntu)" >> $GITHUB_STEP_SUMMARY
        echo "- **Compiler**: MinGW-w64 GCC" >> $GITHUB_STEP_SUMMARY
        echo "- **Linking**: Static (no external dependencies)" >> $GITHUB_STEP_SUMMARY
        echo "- **Compression**: UPX compressed" >> $GITHUB_STEP_SUMMARY
        echo "- **Build Type**: Release" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📚 Dependencies (statically linked):" >> $GITHUB_STEP_SUMMARY
        echo "- libcurl" >> $GITHUB_STEP_SUMMARY
        echo "- OpenSSL" >> $GITHUB_STEP_SUMMARY
        echo "- LibXml2" >> $GITHUB_STEP_SUMMARY
        echo "- Windows system libraries" >> $GITHUB_STEP_SUMMARY
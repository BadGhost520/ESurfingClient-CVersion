name: Build OpenWrt 24.10.5 ramips-mt7621 Package with Dependencies

permissions:
  actions: write
  contents: read

on:
  workflow_dispatch:

jobs:
  build-openwrt-offline-repo:
    runs-on: ubuntu-latest
    container:
      image: debian:11

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install build dependencies
        run: |
          PACKAGES="build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-distutils rsync wget ca-certificates unzip file zstd python3-setuptools swig"
          apt-get update && apt-get install -y $PACKAGES

      - name: Download and extract SDK
        run: |
          SDK_URL="https://mirrors.sustech.edu.cn/openwrt/releases/24.10.5/targets/ramips/mt7621/openwrt-sdk-24.10.5-ramips-mt7621_gcc-13.3.0_musl.Linux-x86_64.tar.zst"
          wget "$SDK_URL"
          tar -I zstd -xf openwrt-sdk-24.10.5-ramips-mt7621_gcc-13.3.0_musl.Linux-x86_64.tar.zst
          mv openwrt-sdk-24.10.5-ramips-mt7621_gcc-13.3.0_musl.Linux-x86_64 openwrt-sdk

      - name: Copy package source to SDK
        run: |
          # 复制你的程序包
          cp -r esurfingclient openwrt-sdk/package/
          
          # 如果有本地源码压缩包，也复制过去
          if [ -f esurfingclient.tar.gz ]; then
            cp esurfingclient.tar.gz openwrt-sdk/dl/
          fi

      - name: Configure build
        run: |
          cd openwrt-sdk
          
          # 更新 feeds
          scripts/feeds update -a
          scripts/feeds install -a
          
          # 使用默认配置 + 选中需要的包
          echo "CONFIG_TARGET_ramips_mt7621=y" >> .config
          echo "CONFIG_TARGET_ramips_mt7621_DEVICE_generic=y" >> .config
          
          # 选中你的包（会自动选中依赖）
          echo "CONFIG_PACKAGE_esurfingclient=y" >> .config
          echo "CONFIG_PACKAGE_libcurl=y" >> .config
          echo "CONFIG_PACKAGE_libopenssl=y" >> .config
          echo "CONFIG_PACKAGE_ca-bundle=y" >> .config
          
          # 关闭内核配置检查（加速编译）
          echo "CONFIG_KERNEL_KALLSYMS=n" >> .config
          echo "CONFIG_KERNEL_DEBUG_KERNEL=n" >> .config
          
          # 生成完整配置
          make defconfig

      - name: Compile all packages
        run: |
          cd openwrt-sdk
          # 编译你的包（会自动编译依赖）
          make package/esurfingclient/compile V=sc -j$(nproc)

      - name: Collect all IPK files
        run: |
          cd openwrt-sdk
          
          # 创建离线仓库目录
          mkdir -p offline-repo
          
          # 查找所有生成的 IPK 文件
          find bin/packages -name "*.ipk" -exec cp {} offline-repo/ \;
          
          echo "📦 Collected packages:"
          ls -la offline-repo/

      - name: Generate repository index
        run: |
          cd openwrt-sdk/offline-repo

          # 检查脚本位置并执行
          if [ -f "../scripts/ipkg-make-index.sh" ]; then
              echo "使用 scripts/ipkg-make-index.sh"
              ../scripts/ipkg-make-index.sh . > Packages 2>/dev/null
          elif [ -f "../staging_dir/host/bin/ipkg-make-index.sh" ]; then
              echo "使用 staging_dir/host/bin/ipkg-make-index.sh"
              ../staging_dir/host/bin/ipkg-make-index.sh . > Packages 2>/dev/null
          else
              echo "⚠️ 找不到 ipkg-make-index.sh，手动生成 Packages"
              # 手动生成
              for ipk in *.ipk; do
                  name=$(basename "$ipk" .ipk)
                  # 提取包名和版本（从 ipk 文件名）
                  pkg_name=$(echo "$name" | sed 's/_[0-9].*$//')
                  pkg_version=$(echo "$name" | sed 's/^[^_]*_//')

                  echo "Package: $pkg_name" >> Packages
                  echo "Version: $pkg_version" >> Packages
                  echo "Filename: $ipk" >> Packages
                  echo "Size: $(stat -c%s "$ipk")" >> Packages
                  echo "MD5Sum: $(md5sum "$ipk" | cut -d' ' -f1)" >> Packages
                  echo "" >> Packages
              done
          fi

          gzip -9nc Packages > Packages.gz
          echo "✅ Repository index generated"

          # 验证
          ls -la Packages Packages.gz

      - name: Create final offline bundle
        run: |
          # 创建最终的压缩包
          BUNDLE_NAME="esurfingclient-offline-repo-ramips-mt7621"
          
          mv openwrt-sdk/offline-repo $BUNDLE_NAME
          
          # 添加安装说明
          cat > $BUNDLE_NAME/README.txt << 'EOF'
          ========================================
          ESurfingClient Offline Repository
          ========================================
          
          安装方法：
          
          1. 将此文件夹上传到路由器（例如 /tmp/repo）
          
          2. 配置 opkg 使用本地源：
             opkg update
             opkg install esurfingclient
          
          3. 或者直接安装（不配置源）：
             opkg install ./esurfingclient_*.ipk
          
          注意：依赖包会自动安装，无需手动处理
          ========================================
          EOF
          
          tar -czf $BUNDLE_NAME.tar.gz $BUNDLE_NAME
          
          echo "✅ Offline bundle created: $BUNDLE_NAME.tar.gz"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: esurfingclient-offline-repo-ramips-mt7621
          path: esurfingclient-offline-repo-ramips-mt7621.tar.gz
          retention-days: 30
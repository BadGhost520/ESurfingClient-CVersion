name: Download Cache

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:

jobs:
  download:
    runs-on: ubuntu-latest
    container:
      image: debian:13

    steps:
      - uses: actions/checkout@v6

      - name: 获取缓存
        id: cache-openwrt-sdk
        uses: actions/cache@v5
        with:
          path: |
            openwrt-sdk-qualcommax-ipq60xx_gcc-13.3.0_musl.Linux-x86_64.tar.zst
          key: openwrt-qualcommax_ipq60xx-ipk-sdk-24.10.6
          restore-keys: |
            openwrt-qualcommax_ipq60xx-ipk
            openwrt-qualcommax_ipq60xx-

      - name: 上传工件
        uses: actions/upload-artifact@v7
        with:
          name: download
          path: openwrt-sdk-qualcommax-ipq60xx_gcc-13.3.0_musl.Linux-x86_64.tar.zst
          retention-days: 30
